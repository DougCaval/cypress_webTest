{
  "name": "saucedemo-cypress-tests",
  "version": "1.0.0",
  "description": "Testes automatizados para o Sauce Demo usando Cypress + Allure",
  "main": "index.js",
  "scripts": {
    "test": "cypress run",
    "test:open": "cypress open",
    "allure:report": "allure generate allure-results --clean -o allure-report",
    "allure:open": "allure open allure-report"
  },
  "keywords": [
    "cypress",
    "saucedemo"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.13.2",
    "cypress": "^13.0.0",
    "@badeball/cypress-cucumber-preprocessor": "^16.0.0",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.0.0",
    "@badeball/cypress-cucumber-preprocessor/esbuild": "^16.0.0"
  }
}
